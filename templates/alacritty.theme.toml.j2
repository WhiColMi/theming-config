[colors.primary]
background = "{{ bg }}"
foreground = "{{ fg }}"

[colors.cursor]
text = "{{ bg }}"
cursor = "{{ accent }}"

[colors.selection]
text = "{{ bg }}"
background = "{{ accent }}"

[colors.normal]
black = "{{ dark }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ blue }}"
magenta = "{{ purple }}"
cyan = "{{ orange }}"
white = "{{ light }}"

[colors.bright]
black = "{{ dark }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ blue }}"
magenta = "{{ purple }}"
cyan = "{{ orange }}"
white = "{{ light }}"

