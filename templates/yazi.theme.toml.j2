# Yazi Terminal Configuration

# General colors
background = "{{ bg }}"
foreground = "{{ fg }}"

[cursor]
text = "{{ bg }}"
cursor = "{{ accent }}"

[selection]
text = "{{ bg }}"
background = "{{ accent }}"

# ANSI colors
[colors]
black = "{{ dark }}"
red = "{{ red }}"
green = "{{ green }}"
yellow = "{{ yellow }}"
blue = "{{ blue }}"
magenta = "{{ purple }}"
cyan = "{{ orange }}"
white = "{{ light }}"

brightBlack = "{{ dark }}"
brightRed = "{{ red }}"
brightGreen = "{{ green }}"
brightYellow = "{{ yellow }}"
brightBlue = "{{ blue }}"
brightMagenta = "{{ purple }}"
brightCyan = "{{ orange }}"
brightWhite = "{{ light }}"
